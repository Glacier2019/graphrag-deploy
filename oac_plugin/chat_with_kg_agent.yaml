default:
  logger:
    log_level: "DEBUG"
  service:
    host: "0.0.0.0"
    port: 8283
    cert_file: "ssl_certs/localhost.crt"
    cert_key: "ssl_certs/localhost.key"

  history:
    retention_mode: "by_both"
    max_events: 1000
    max_age_seconds: 3600
    cleanup_interval_seconds: 30

  chat_engine:
    model_root: "models"
    concurrent_limit: 1
    handler_search_path:
      - "src/handlers"
      - "/data/cy/graphrag-deploy/oac_plugin"
    handler_configs:
      RtcClient:
        module: client/rtc_client/client_handler_rtc
        connection_ttl: 900
        output_video_fps: 25

      InterruptHandler:
        module: logic/interrupt/interrupt_handler

      SileroVad:
        module: vad/silerovad/duplex_vad_handler
        speaking_threshold: 0.25
        start_delay: 1024
        end_delay: 32000
        early_end_delay: 5000
        early_end_repeat_delay: 1000
        buffer_look_back: 2048
        speech_padding: 512
        volume_threshold: -50
        post_end_monitor_samples: 32000
        reconnect_threshold_samples: 32000
        post_end_energy_threshold: -5

      SenseVoice:
        enabled: True
        module: asr/sensevoice/asr_handler_sensevoice
        model_name: "iic/SenseVoiceSmall"
        input_type_override:
          HUMAN_AUDIO: HUMAN_DUPLEX_AUDIO
        output_type_override:
          HUMAN_TEXT: HUMAN_DUPLEX_TEXT

      SmartTurnEOU:
        enabled: true
        module: vad/smart_turn_eou/eou_handler_smart_turn
        threshold: 0.8
        max_buffer_seconds: 300.0
        sample_rate: 16000
        model_path: "models/smart_turn/smart-turn-v3.1-cpu.onnx"
        input_type_override:
          HUMAN_AUDIO: HUMAN_DUPLEX_AUDIO

      SemanticTurnDetector:
        enabled: true
        module: llm/semantic_turn_detector/semantic_turn_detector_handler
        duplex_mode: true
        api_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
        model_name: "qwen-plus"
        enable_interrupt_detection: true
        interrupt_on_any_speech: false
        min_text_length_for_interrupt: 2
        enable_completion_detection: false
        min_text_length_for_completion: 1
        interrupt_judge_api_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
        interrupt_judge_model_name: "qwen-plus"
        request_timeout: 3.0
        max_context_turns: 3
        # ★ 输出改道 HUMAN_AUDIO（此配置中无其他消费者）
        output_type_override:
          HUMAN_TEXT: HUMAN_AUDIO

      # ═══════════════════════════════════════════════
      # 知识图谱富化器 — text 模式串联管道
      # SemanticTurnDetector → HUMAN_AUDIO → KgEnrich → HUMAN_TEXT → ChatAgent
      # ★ HUMAN_AUDIO 作中间桥接，ChatAgent 只接收富化后的文本
      # ═══════════════════════════════════════════════
      KgEnrich:
        enabled: true
        module: kg_enrich_handler
        output_mode: text
        mcp_url: "http://localhost:8011"
        mcp_timeout: 3.0
        max_entities: 2
        max_relations: 3
        context_max_chars: 400
        min_interval_seconds: 3.0
        # ★ 接收 SemanticTurnDetector 的 HUMAN_AUDIO
        input_type_override:
          HUMAN_TEXT: HUMAN_AUDIO

      CosyVoice:
        enabled: True
        module: tts/bailian_tts/tts_handler_cosyvoice_bailian
        voice: "longxing_v3"
        model_name: "cosyvoice-v3-flash"

      PerceptionAgent:
        enabled: True
        module: agent/perception/perception_handler
        summary_interval: 5.0
        max_buffer_frames: 30
        key_frame_strategy: "interval"
        key_frame_interval: 10
        vision_model_type: "openai"
        llm_model: "qwen3-vl-flash"
        api_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
        max_frames: 8
        enable_event_detection: True
        event_detection_interval: 5
        max_concurrent_requests: 3

      ChatAgent:
        enabled: True
        module: agent/chat_agent_handler
        llm_model: "qwen3.6-plus"
        api_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
        stable_core: |
          你是一个具有视觉能力的实时数字人助手，正在通过摄像头与用户进行面对面对话。

          ## 输出约束
          - 用口语化的自然语言回答，语气亲切
          - 保持简短（通常 2-3 句），除非用户明确要求详细说明
          - 不要输出 Markdown 格式、代码块或列表符号
          - 不要自称"AI"或"语言模型"，你就是这个数字人角色本身

          ## 视觉规则
          - <environment-state> 标签描述的是你当前看到的持续性环境状态
          - 可以自然地提及观察到的内容，但不要每句都提

          ## 知识图谱使用
          你连接了一个知识图谱（GraphRAG）。用户消息中可能包含"知识图谱参考"信息块，
          其中包含来自后台知识库的实体和关联信息。
          - 当用户的问题涉及图谱中的实体时，将相关信息自然融入回答
          - 图谱信息权威性高于你的训练数据
          - 不要在回答中逐字复述图谱信息，用自己的话概括

          ## 结构化标签说明
          - <environment-state>: 当前环境的持续状态描述，作为背景知识理解
          - <observation event="true">: 刚发生的环境事件
          - <observation compact="true">: 历史感知摘要
          - <dialogue-summary>: 之前对话的压缩摘要
        persona_snapshot: |
          性格：友善亲切，像朋友一样交流。观察细致，能注意到用户状态变化。
          口吻：轻松自然，适当关心用户情绪。
        persona_max_chars: 2500
        perception_max_chars: 800
        perception_max_entries: 100
        perception_decay_rate: 0.1
        max_dialogue_turns: 20
        context_compact:
          enabled: true
          compact_threshold: 15
          keep_recent: 5
          save_transcript: true
          rehydrate_task_brief: true
          rehydrate_env_state: true
        tool_use:
          enabled: true
          max_tool_rounds: 5
          register_demo_tools: true
        oc_bridge:
          enabled: true
          plugin_tools_cmd: "node /home/luyi/Code/openclaw/dist/mcp/plugin-tools-serve.js"
          gateway_http_url: "http://localhost:18789"
          callback_port: 8011
        proactive:
          enabled: true
          event_triggers:
            waving:
              enabled: true
              cooldown: 30.0
              hint: "请友好地回应用户的招呼，可以适当询问有什么可以帮助的。"
            showing_object:
              enabled: true
              cooldown: 30.0
              hint: "用户正在展示物品，请主动询问或评论用户展示的物品。"
            asking_for_attention:
              enabled: true
              cooldown: 30.0
              hint: "用户在寻求你的注意，请积极回应并询问需要什么帮助。"
            arriving:
              enabled: true
              cooldown: 60.0
              hint: "有人进入画面，可以适当欢迎。"
            leaving:
              enabled: true
              cooldown: 60.0
              hint: "用户似乎要离开，可以适当道别。"
          idle_trigger:
            enabled: false
            idle_seconds: 60.0
            hint: "用户已经安静了一会儿，可以适当关心一下或轻松地聊点什么。"
            mode_overrides:
              companion: 120.0
              office: 300.0

      FlashHead:
        module: avatar/flashhead/avatar_handler_flashhead
        ckpt_dir: "models/SoulX-FlashHead-1_3B"
        wav2vec_dir: "models/wav2vec2-base-960h"
        model_type: "lite"
        cond_image_path: "resource/avatar/flashhead/girl.png"
        fps: 25
        base_seed: 42
        use_face_crop: false
        debug: true
        idle_noise_amplitude: 0.003
